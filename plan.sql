use airbnb_ny;
SELECT *
FROM ab_nyc_2019;

-- PLAN
-- Cleaning Data
-- Which host has the most offerts?
-- Average amount offerts per host 
-- Which neighbourhood_group has the most offerts?
-- Which neighbourhood has the most offerts?
-- Which room type has the most offerts?
-- Which room type is the most expensive by neighbourhood and room type?
-- Average price per night in each specific neighbourhood
-- Average minimum nights required in each specific neighbourhood
-- Comparison of average price per night to average minimum nights
-- Where was the most offerts availability by 365 days?
-- Average availability of offerts 
-- In which year and month didhost gather the most reviews?